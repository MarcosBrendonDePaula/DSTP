// Reuse the generic action handler (reads the fixed action_type from meta.defaults):
// the params go straight to the mod's `entity_set_brain` command.
export { handler } from '../../game/action/exec'
